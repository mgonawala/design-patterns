package Collection.QueuesExample;

/**
 *
 * Queue -> First In Fist out DS.
 *
 * add(E) -> returns exception if queue is full.
 * offer(E) -> returns false if queue is full.
 *
 * poll(E) -> returns null if queue is empty.
 * remove(E) -> returns exception is queue is empty
 *
 * Element(E) -> returns exception is queue is empty.
 * peek(E) ->  returns null is queue is empty.
 *
 * Deque interface methods.
 *
 * addFirst(E) -> adds the elements at the front of the deque or
 *              returns exception if queue is full.
 * addLast(E) -> adds the element at the last of the deque
 *          or returns exception if queue is full.
 *
 * offerFirst(E) -> adds element at the front of the deque
 *              returns false if queue is full.
 * offerLast(E) -> adds element at the last of the deque
 *              returns false if queue is full.
 *
 *
 *         removeFirst() -> throws exception
 *         removeLast() -> returns false
 *
 *         pollFirst() -> throws exception
 *         pollLast() -> returns false
 *
 *
 *  Deque also has push & pop methods.
 *
 *  push(E)
 *  pop()
 *
 *  you can use this if you want to use stack.
 */
public class QueueExample {
}
